spring:
  datasource:
    url: jdbc:postgresql://localhost:30001/charging_station
    username: cs_user
    password: dev_password_123
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: dev-secret-key-not-for-production-use-32chars!!